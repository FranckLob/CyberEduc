# CyberEduc
Implementation of jwt. 
Authentication and Authorization. 

## Swagger (no need to be authenticated)
Definition in json format is accessible at :    http://localhost:8080/v3/api-docs. 
Definition in yaml format is accessible at :    http://localhost:8080/v3/api-docs.yaml. 

## Postgresql
To launch the database : docker-compose -f docker-compose.yml up -d